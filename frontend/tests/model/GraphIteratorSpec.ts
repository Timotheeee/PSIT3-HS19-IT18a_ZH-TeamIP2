import { MyGraphIterator } from "./../../src/model/MyGraphIterator";


describe("MyGraphIterator", () => {    
    let graphIterator: GraphIterator = null;

    beforeEach(() => {
        
    });
});
